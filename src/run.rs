use xt_type::*;
use parser::*;

pub fn interprete(ast: AST) -> Result<isize, isize>{
    println!("=========interpreter start========");
    //
    println!("==========interpreter end=========");
    Result::Ok(0)
}